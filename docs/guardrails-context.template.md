# Repository context for AI agents

Generated by `scripts/configure_context.sh` (or `scripts/init.sh`). Edit the marked sections or re-run init.

Agents: when classifying risk, treat paths listed under **High-risk paths** as **High** or **Critical** minimum unless the change is documentation-only.

## High-risk paths

# guardrails-high-risk-start
(none configured — run: bash scripts/configure_context.sh)
# guardrails-high-risk-end

## Stack notes

# guardrails-stack-notes-start
(not configured)
# guardrails-stack-notes-end

## Monorepo layout

# guardrails-monorepo-start
(not detected)
# guardrails-monorepo-end
